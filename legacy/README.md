# Legacy (v1 snapshot)

Frozen copy of the pre-v2 BacksterOS tree. **Do not develop here.**

Use this folder as a reference when porting features into:

- `core/` — API + shared contracts
- `mobile/` — Expo (iPhone + iPad)
- `desktop/` — Tauri + React

Active work happens outside `legacy/`.
