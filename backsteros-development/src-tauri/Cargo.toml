[package]
name = "backsteros-development"
version = "0.1.0"
description = "Thin native window for BacksterOS Development (loads local Next.js)"
authors = ["BacksterOS"]
edition = "2021"

[lib]
name = "backsteros_development_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
