/** Max PDF / attachment upload size (matches route handlers). */
export const MAX_UPLOAD_BYTES = 25_000_000;
/** Max avatar upload size (matches route handlers). */
export const MAX_AVATAR_BYTES = 5_000_000;
/** Max task description image upload size (matches route handlers). */
export const MAX_TASK_IMAGE_BYTES = 10_000_000;
