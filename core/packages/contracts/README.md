# @backsteros/contracts

Shared Zod schemas and ts-rest API contract for BacksterOS.

**Status:** Phase 1 — tasks, projects, API keys.

Specs: [`../../docs/04-api-and-sync.md`](../../docs/04-api-and-sync.md)

Used by:

- `backsteros-api` — implements routes + OpenAPI
- Future `api-client` — generated HTTP client (Phase 1+)
