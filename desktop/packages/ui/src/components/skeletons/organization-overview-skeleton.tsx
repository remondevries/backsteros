import { ProjectOverviewSkeleton } from "./project-overview-skeleton.js";

/** Organization overview — reuses project overview chrome. */
export function OrganizationOverviewSkeleton() {
  return <ProjectOverviewSkeleton />;
}
