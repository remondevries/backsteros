# API origin (no trailing slash)
VITE_API_URL=http://127.0.0.1:8787

# Clerk publishable key (same instance as product; owner-only UI later)
VITE_CLERK_PUBLISHABLE_KEY=

# Production base path is /admin/ (set in vite.config). Local override:
# VITE_BASE_PATH=/
